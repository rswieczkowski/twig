<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
</head>
<style>
    table, th, td {
        border: 1px solid black
    }
</style>

<body>
<table>


    <tr>
        <th>Product</th>
        <th>Description</th>
        <th>Value</th>
        <th>Date</th>
    </tr>

    <tbody>
    {% for product in products %}

            <tr>
                <td>{{ product.name | striptags | title }}</td>
                <td>{{ product.description }}</td>
                <td>{{ product.value }}</td>
                <td>{{ product.date_register|date('m/d/Y') }}</td>
            </tr>

    {% endfor %}
    </tbody>

</table>


</body>
</html>