<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Date</title>
</head>

<body>

{% if date(user.created_at) < date('-5years') %}

    <p>User {{ user.name }} is a senior user!</p>
{% endif %}
</body>
</html>