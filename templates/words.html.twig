<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Custom filter</title>
</head>

<body>

<ul>
    {% for word in  words %}
        <li> {{ word }} </li>
    {% endfor %}
</ul>

<ul>
    {% for word in words|slice(2, 3) %}
        <li>{{ word }}</li>

    {% endfor %}
</ul>

</body>
</html>